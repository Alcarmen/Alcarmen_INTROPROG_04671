<!DOCTYPE html>
<html>

<head>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <style>
    body {
      font-family: Arial, Helvetica, sans-serif;
    }

    div.gallery-container {
      display: flex;
      flex-wrap: wrap;
    }

    div.gallery {
      margin: 5px;
      border: 1px solid #ccc;
      border-radius: 5px solid red;
      width: 180px;
      overflow: hidden;
    }

    div.gallery:hover {
      border: 1px solid #777;
      border-radius: 5px solid red;
    }

    div.gallery img {
      width: 100%;
      height: auto;
      border-radius: 5px;
      cursor: pointer;
      transition: 0.3s;
    }

    div.gallery img:hover {
      opacity: 0.7;
    }
    div.desc{
	  text-align:center;
	}

    /* The Modal (background) */
    .modal {
      display: none; /* Hidden by default */
      position: fixed; /* Stay in place */
      z-index: 1; /* Sit on top */
      padding-top: 100px; /* Location of the box */
      left: 0;
      top: 0;
      width: 100%; /* Full width */
      height: 100%; /* Full height */
      overflow: auto; /* Enable scroll if needed */
      background-color: rgb(0, 0, 0); /* Fallback color */
      background-color: rgba(0, 0, 0, 0.9); /* Black w/ opacity */
    }

    /* Modal Content (image) */
    .modal-content {
      margin: auto;
      display: block;
      width: 80%;
      max-width: 700px;
    }

    /* Caption of Modal Image */
    #caption {
      margin: auto;
      display: block;
      width: 80%;
      max-width: 700px;
      text-align: center;
      color: #ccc;
      padding: 10px 0;
      height: 150px;
    }

    /* Add Animation */
    .modal-content,
    #caption {
      -webkit-animation-name: zoom;
      -webkit-animation-duration: 0.6s;
      animation-name: zoom;
      animation-duration: 0.6s;
    }

    @-webkit-keyframes zoom {
      from {
        -webkit-transform: scale(0)
      }

      to {
        -webkit-transform: scale(1)
      }
    }

    @keyframes zoom {
      from {
        transform: scale(0)
      }

      to {
        transform: scale(1)
      }
    }

    /* The Close Button */
    .close {
      position: absolute;
      top: 15px;
      right: 35px;
      color: #f1f1f1;
      font-size: 40px;
      font-weight: bold;
      transition: 0.3s;
    }

    .close:hover,
    .close:focus {
      color: #bbb;
      text-decoration: none;
      cursor: pointer;
    }

    /* 100% Image Width on Smaller Screens */
    @media only screen and (max-width: 700px) {
      .modal-content {
        width: 100%;
      }
    }
  </style>
</head>

<body>

  <h2>Image Modal</h2>
  <p>In this example, we use CSS to create a modal (dialog box) that is hidden by default.</p>
  <p>We use JavaScript to trigger the modal and to display the current image inside the modal when it is clicked on.
    Also, note that we use the value from the image's "alt" attribute as an image caption text inside the modal.</p>

  <!-- Gallery Container -->
  <div class="gallery-container">
    <!-- Album Images -->
    <div class="gallery" onclick="openModal('https://i.pinimg.com/originals/92/e5/cd/92e5cdc6e46274eb2425c6dee04c85ca.jpg', 'Kakayot')">
      <img src="img_5terre.jpg" alt="Cinque Terre" width="600" height="400">
      <div class="desc">This is Brandon House</div>
    </div>

    <div class="gallery" onclick="openModal('https://w7.pngwing.com/pngs/441/873/png-transparent-dragon-ball-z-beerus-illustration-beerus-goku-vegeta-dragon-ball-heroes-goten-lord-purple-fictional-character-cartoon-thumbnail.png', 'Violet Kangaroo')">
      <img src="img_forest.jpg" alt="Forest" width="600" height="400">
      <div class="desc">Forest Toilet</div>
    </div>

    <div class="gallery" onclick="openModal('https://w7.pngwing.com/pngs/284/669/png-transparent-vegeta-goku-dragon-ball-z-dokkan-battle-trunks-gohan-vegeta-superhero-cartoon-fictional-character-thumbnail.png', 'Foreheaded Blondie')">
      <img src="img_lights.jpg" alt="Northern Lights" width="600" height="400">
      <div class="desc">Laser Blits</div>
    </div>

    <div class="gallery" onclick="openModal('img_mountains.jpg', 'Mountains')">
      <img src="img_mountains.jpg" alt="Mountains" width="600" height="400">
      <div class="desc">Pee Station</div>
    </div>
  </div>

  <!-- The Modal -->
  <div id="myModal" class="modal">
    <span class="close" onclick="closeModal()">&times;</span>
    <img class="modal-content" id="img01">
    <div id="caption"></div>
  </div>

  <script>
    function openModal(imgSrc, imgAlt) {
      var modal = document.getElementById("myModal");
      var modalImg = document.getElementById("img01");
      var captionText = document.getElementById("caption");

      modal.style.display = "block";
      modalImg.src = imgSrc;
      captionText.innerHTML = imgAlt;
    }

    function closeModal() {
      var modal = document.getElementById("myModal");
      modal.style.display = "none";
    }
  </script>

</body>

</html>
